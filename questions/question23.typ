= Ориентированные и неориентированные графы.

*Ориентированным графом (орграфом)* называют $arrow(G) = (V, alpha)$, где $V$ --- конечное не пустое множество вершин, элементы которого называют вершинами, а $alpha subset.eq V times V$ --- бинарное отношение на множестве $V$, которое называют *отношением смежности*, а соответствующую двоичную матрицу этого отношения --- *матрицей смежности*

*Дугой орграфа* называют пару $(u, v) in alpha$, это дуга орграфа с началом в вершине $u$ и концом в вершине $v$.

*Неориентированным графом* называют $G = (V, alpha)$, где $alpha$ --- это симметричное и антирефлексивное бинарное отношение на множестве вершин $V$.

*Ребро неориентированного графа* --- соответствующая дуга этого графа. Ребро образованное вершинами $u$ и $v$ обозначается как ${u, v}$. Вершины являющиеся концами ребра называют *смежными*.
